class Config {
  final double saturation;
  final colorLightness;
  final grayscaleLightness;
  final String backColor;

  Config(this.saturation, this.colorLightness, this.grayscaleLightness,
      this.backColor);
}
